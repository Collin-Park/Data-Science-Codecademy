SELECT ROUND(AVG(price), 2)
FROM fake_apps;

/*
round take in two arguments, the first argument is the columns over which the aggregate function operates and the second argument is an integer which specifies the number of decimal points that the number will be rounded to
*/
