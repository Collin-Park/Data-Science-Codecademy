/*
to count rows:
SELECT COUNT(*) FROM fake_apps;

to sum over rows:
SELECT SUM(downloads)
FROM fake_apps;

to find max and min:
SELECT MAX(price)
FROM fake_apps;

to find average:
SELECT AVG(price)
FROM fake_apps;

to round:
SELECT ROUND(AVG(price), 2)
FROM fake_apps;

*/
