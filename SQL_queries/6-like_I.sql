SELECT *
FROM movies
WHERE name LIKE 'Se_en';

/*
LIKE is a operator which compares like values, here we can use wildcard values like _ to find movie titles with the stings matching bar the wildcard which can take any value
*/
