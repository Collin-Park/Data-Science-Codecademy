SELECT *
FROM movies
WHERE name LIKE 'The%';

/*underscore is a wildcard that replaces one string value, % sign replaces all values taht follow. The% returns all movies with the name that start with The*/
